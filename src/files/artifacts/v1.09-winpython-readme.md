# DCO Daily Check – v1.09 WinPython README update

## Context
- Branch: `feature/winpython-readme`
- Goal: document the verified WinPython portable installation workflow to ensure packages remain inside the bundle (offline-ready).

## Changes
1. **README.md**
   - Added a “Verified portable installation workflow” subsection after the existing WinPython guidance.
   - Documented the full sequence: pre/post `pip list --path` checks, setting `PYTHONNOUSERSITE=1`, and using `pip install -r requirements-verified.txt --target <WinPython site-packages>`.
2. **src/DCO-DailyCheck.py**
   - Bumped `__version__` to `1.09` and appended a changelog line referencing the README update.

## Validation
- Documentation-only changes; the repository does not define automated tests. Verified command syntax and paths against the existing pinned requirements workflow.

## Files touched
- `README.md`
- `src/DCO-DailyCheck.py`
- `src/files/artefacts/v1.09-winpython-readme.md`

## Next steps
- Await user approval before committing or pushing.
- Once approved, commit, push, and tag `v1.09` following the established workflow.
