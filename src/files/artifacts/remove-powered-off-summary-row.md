# MISSION: Remove "VMs Powered Off" row from summary report

The objective was to modify the Daily Check (DC) report generation for vCenter to exclude the row that displays the count of powered-off VMs in the "System Summary" table.

## Changes Implemented

### src/VC/VCcreatereportDC.py
- Updated `create_DC` function to load the CSV into a raw DataFrame.
- Filtered out the "VMs Powered Off" row.
- Removed unused styling logic.

```diff
--- a/src/VC/VCcreatereportDC.py
+++ b/src/VC/VCcreatereportDC.py
@@ -16,18 +16,7 @@
     return color_map.get(val, "")
 
 
-def color_off_vms(val):
-    """
-    Red color if there are powered off VMs (val > 0).
-    """
-    try:
-        count = int(val)
-        return DCOreport.PASTEL_RED if count > 0 else DCOreport.PASTEL_GREEN
-    except:
-        return ''
-
 def create_DC(dcocfg, dcorpt):
-
     """
     Entry point to add the vCenter summary table to the Daily Check report.
     """
@@ -34,7 +34,13 @@
         # Load the summary dataframe generated by processinfo
-        summary_df = DCOreport.csv_to_styleddf(system, instance, "systemSummary", dcocfg)
+        df = dcocfg.load_csv_to_dataframe(system, instance, "systemSummary")
         
-        if not summary_df.data.empty:
+        if not df.empty:
+            # Filter out the "VMs Powered Off" row as requested
+            df = df[df.iloc[:, 0] != "VMs Powered Off"]
+            
+            # Apply base styling
+            summary_df = DCOreport.table_base_styler(df)
+            
             # 1. Apply status coloring to specific status rows
             summary_df = DCOreport.format_by_rowid(summary_df, [
                 ("Appliance Health", color_status),
```
